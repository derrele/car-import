"use client";
import { useMemo, useState } from "react";
import { AlertTriangle, Calculator, CarFront, CircleCheck, Euro, Gauge, Sparkles, TrendingUp } from "lucide-react";
import { PageHeader } from "@/components/layout/page-header";
import { analyzeVehicle } from "@/lib/calculation-engine";
import { defaultSettings } from "@/data/settings/default-settings";
const profile=defaultSettings.costProfiles[0];
const initial={make:"",model:"",year:"",mileage:"",purchasePrice:"",salePrice:"",repairs:"0"};
function MoneyInput({label,value,onChange,help}:{label:string;value:string;onChange:(value:string)=>void;help?:string}){return <label className="block"><span className="text-sm font-semibold">{label}</span>{help&&<span className="ml-2 text-xs text-muted">{help}</span>}<div className="relative mt-1.5"><input inputMode="decimal" type="number" min="0" value={value} onChange={e=>onChange(e.target.value)} className="w-full rounded-xl border bg-background px-3 py-3 pr-10 text-sm font-semibold outline-none focus:border-emerald-500 focus:ring-2 focus:ring-emerald-500/20"/><span className="pointer-events-none absolute inset-y-0 right-3 grid place-items-center text-sm text-muted">€</span></div></label>}
const Metric=({label,value,accent=false}:{label:string;value:string;accent?:boolean})=><div className={`rounded-xl p-4 ${accent?"bg-emerald-500 text-white":"bg-background"}`}><p className={`text-xs font-medium ${accent?"text-emerald-50":"text-muted"}`}>{label}</p><p className="mt-1 text-xl font-bold tracking-tight">{value}</p></div>;
export function QuickCalculator(){const [form,setForm]=useState(initial);const [submitted,setSubmitted]=useState(false);const change=(key:keyof typeof initial)=>(value:string)=>setForm(current=>({...current,[key]:value}));const result=useMemo(()=>{const purchasePrice=Number(form.purchasePrice),expectedSalePrice=Number(form.salePrice);if(!submitted||purchasePrice<=0||expectedSalePrice<=0)return null;return analyzeVehicle({purchasePrice,expectedSalePrice,holdingDays:defaultSettings.targets.targetSaleDays,annualCapitalCostRate:defaultSettings.targets.annualCapitalCost,costs:{transport:profile.transport,itvAndHomologation:profile.itv,agency:profile.agency,registration:profile.plates,preparation:profile.preparation,repairs:Number(form.repairs),contingency:profile.contingency,other:profile.inspection},targetRoi:defaultSettings.targets.targetRoi})},[form,submitted]);const format=(value:number)=>`${value.toLocaleString("ca-ES",{maximumFractionDigits:0})} €`;const isGood=result&&result.adjustedProfit>=defaultSettings.targets.minimumProfit&&result.roi>=defaultSettings.targets.minimumRoi;return <><PageHeader eyebrow="Anàlisi" title="Calculadora ràpida" description="Introdueix les dades essencials i obtén una primera lectura de la rendibilitat. Els costos fan servir el perfil de Transportista configurat."/><div className="grid gap-6 xl:grid-cols-[.95fr_1.05fr]"><section className="rounded-2xl border bg-card p-5 shadow-sm sm:p-6"><div className="flex items-center gap-2"><CarFront size={19} className="text-emerald-600"/><h2 className="font-bold">Vehicle i operació</h2></div><p className="mt-1 text-sm text-muted">Pots deixar les dades descriptives buides per fer una simulació ràpida.</p><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="block"><span className="text-sm font-semibold">Marca</span><input value={form.make} onChange={e=>change("make")(e.target.value)} placeholder="Ex. BMW" className="mt-1.5 w-full rounded-xl border bg-background px-3 py-3 text-sm outline-none focus:border-emerald-500"/></label><label className="block"><span className="text-sm font-semibold">Model</span><input value={form.model} onChange={e=>change("model")(e.target.value)} placeholder="Ex. 320d Touring" className="mt-1.5 w-full rounded-xl border bg-background px-3 py-3 text-sm outline-none focus:border-emerald-500"/></label><label className="block"><span className="text-sm font-semibold">Any</span><input type="number" value={form.year} onChange={e=>change("year")(e.target.value)} placeholder="2021" className="mt-1.5 w-full rounded-xl border bg-background px-3 py-3 text-sm outline-none focus:border-emerald-500"/></label><label className="block"><span className="text-sm font-semibold">Quilòmetres</span><input type="number" value={form.mileage} onChange={e=>change("mileage")(e.target.value)} placeholder="65.000" className="mt-1.5 w-full rounded-xl border bg-background px-3 py-3 text-sm outline-none focus:border-emerald-500"/></label></div><div className="mt-6 border-t pt-6"><div className="flex items-center gap-2"><Euro size={19} className="text-emerald-600"/><h2 className="font-bold">Xifres clau</h2></div><div className="mt-4 grid gap-4 sm:grid-cols-2"><MoneyInput label="Preu de compra" value={form.purchasePrice} onChange={change("purchasePrice")}/><MoneyInput label="Preu de venda estimat" value={form.salePrice} onChange={change("salePrice")}/><MoneyInput label="Reparacions previstes" value={form.repairs} onChange={change("repairs")} help="Opcional"/></div><div className="mt-5 rounded-xl bg-background p-4 text-sm"><p className="font-semibold">Costos inclosos del perfil</p><p className="mt-1 text-muted">Transport, ITV/homologació, gestoria, plaques, preparació, revisió i imprevistos: <b className="text-foreground">{format(profile.transport+profile.itv+profile.agency+profile.plates+profile.preparation+profile.inspection+profile.contingency)}</b></p></div></div><button onClick={()=>setSubmitted(true)} disabled={!form.purchasePrice||!form.salePrice} className="mt-6 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-emerald-600 px-5 py-3.5 text-sm font-bold text-white shadow-lg shadow-emerald-900/20 hover:bg-emerald-700 disabled:cursor-not-allowed disabled:bg-slate-300"><Calculator size={18}/>Analitzar operació</button></section><section className="rounded-2xl border bg-card p-5 shadow-sm sm:p-6">{!result?<div className="grid min-h-[420px] place-items-center text-center"><div><span className="mx-auto grid size-12 place-items-center rounded-xl bg-emerald-50 text-emerald-700 dark:bg-emerald-950 dark:text-emerald-300"><Sparkles size={22}/></span><h2 className="mt-4 font-bold">A punt per analitzar</h2><p className="mt-2 max-w-sm text-sm leading-6 text-muted">Afegeix el preu de compra i el preu de venda estimat per veure la rendibilitat de l&apos;operació.</p></div></div>:<><div className={`flex items-start gap-3 rounded-xl p-4 ${isGood?"bg-emerald-50 text-emerald-950 dark:bg-emerald-950/50 dark:text-emerald-100":"bg-amber-50 text-amber-950 dark:bg-amber-950/40 dark:text-amber-100"}`}>{isGood?<CircleCheck className="mt-0.5 shrink-0 text-emerald-600"/>:<AlertTriangle className="mt-0.5 shrink-0 text-amber-600"/>}<div><p className="font-bold">{isGood?"Operació interessant":"Revisa aquesta operació"}</p><p className="mt-1 text-sm">{isGood?"Compleix els objectius mínims configurats.":"No arriba a tots els objectius mínims. Revisa el preu de compra, venda o costos."}</p></div></div><div className="mt-6 flex items-center gap-2"><TrendingUp size={19} className="text-emerald-600"/><h2 className="font-bold">Resultat estimat</h2></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><Metric label="Benefici ajustat" value={format(result.adjustedProfit)} accent/><Metric label="ROI" value={`${result.roi.toFixed(1)} %`}/><Metric label="Cost total" value={format(result.totalCost)}/><Metric label="Marge sobre venda" value={`${result.margin.toFixed(1)} %`}/><Metric label="Cost del capital" value={format(result.capitalCost)}/><Metric label="Benefici per dia" value={format(result.profitPerDay)}/></div><div className="mt-6 rounded-xl border p-4"><div className="flex items-center gap-2"><Gauge size={18} className="text-emerald-600"/><p className="font-semibold">Punt de decisió</p></div><p className="mt-2 text-sm leading-6 text-muted">Per assolir el ROI objectiu del {defaultSettings.targets.targetRoi} %, el preu màxim de compra hauria de ser <b className="text-foreground">{format(result.maxBuyPrice)}</b>. El break-even és <b className="text-foreground">{format(result.breakEven)}</b>.</p></div><div className="mt-5 grid grid-cols-3 gap-2">{result.scenarios.map(item=><div key={item.name} className="rounded-xl bg-background p-3 text-center"><p className="text-xs text-muted">{item.name}</p><p className="mt-1 text-sm font-bold">{format(item.adjustedProfit)}</p><p className="text-xs text-muted">{item.roi.toFixed(1)} % ROI</p></div>)}</div></>}</section></div></>}
