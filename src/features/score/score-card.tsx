import type { ScoreBreakdown } from "@/lib/calculation-engine";
const labels:{key:keyof Omit<ScoreBreakdown,"total">;label:string;maximum:number}[]=[{key:"roi",label:"ROI",maximum:30},{key:"saleSpeed",label:"Velocitat de venda",maximum:20},{key:"absoluteProfit",label:"Benefici absolut",maximum:15},{key:"risk",label:"Risc",maximum:15},{key:"marketPrice",label:"Preu vs mercat",maximum:10},{key:"capitalRequired",label:"Capital necessari",maximum:10}];
const classification=(score:number)=>score>=90?"Excepcional":score>=80?"Molt bona":score>=70?"Interessant":score>=60?"Justa":"Descartar";
export function ScoreCard({score}:{score:ScoreBreakdown}){const rounded=Math.round(score.total);return <section className="mt-6 rounded-2xl border bg-card p-5 shadow-sm"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-emerald-700 dark:text-emerald-300">Car Import Score</p><h2 className="mt-1 text-lg font-bold">{classification(rounded)}</h2></div><div className="grid size-16 place-items-center rounded-full border-4 border-emerald-500 text-xl font-bold">{rounded}</div></div><p className="mt-2 text-xs text-muted">Puntuació transparent sobre 100. El criteri “preu vs mercat” queda a 0 fins que tinguem comparables reals.</p><div className="mt-5 space-y-3">{labels.map(({key,label,maximum})=>{const value=score[key];return <div key={key}><div className="mb-1 flex justify-between text-xs"><span>{label}</span><b>{value.toFixed(1)} / {maximum}</b></div><div className="h-2 overflow-hidden rounded-full bg-slate-100 dark:bg-slate-800"><div className="h-full rounded-full bg-emerald-500" style={{width:`${value/maximum*100}%`}}/></div></div>})}</div></section>}
