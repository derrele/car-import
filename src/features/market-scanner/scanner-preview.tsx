"use client";
import { useMemo, useState } from "react"; import { AlertTriangle, Filter, Radar, Tag, TrendingDown } from "lucide-react"; import { PageHeader } from "@/components/layout/page-header"; import { demoGermanListings } from "@/data/providers/demo-market-provider"; import { analyzeOpportunities } from "@/lib/market-scanner/analyze-opportunities";
const euro=(v:number)=>`${v.toLocaleString("ca-ES",{maximumFractionDigits:0})} €`; const quality=(score:number)=>score>=90?"Excepcional":score>=80?"Molt bona":score>=70?"Interessant":score>=60?"Justa":"Descartar"; const signal={high:"Negociació alta",medium:"Vigilar",none:"Recent"};
export function ScannerPreview(){const [minScore,setMinScore]=useState(0);const opportunities=useMemo(()=>analyzeOpportunities(demoGermanListings).filter(item=>item.score>=minScore),[minScore]);return <><PageHeader eyebrow="Oportunitats" title="Market Scanner" description="Flux preparat per normalitzar anuncis, estimar venda, aplicar costos i detectar baixades de preu."/><div className="mb-6 flex gap-3 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900 dark:border-amber-900 dark:bg-amber-950/40 dark:text-amber-100"><AlertTriangle className="shrink-0" size={19}/><div><b>Mode demostració.</b> Vehicles, preus i històrics ficticis locals. No hi ha connexió amb Mobile.de, portals espanyols ni scraping.</div></div><section className="rounded-2xl border bg-card p-5 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-4"><div className="flex items-center gap-2"><Filter size={18} className="text-emerald-600"/><div><h2 className="font-bold">Filtres</h2><p className="text-sm text-muted">Ranking de demostració.</p></div></div><label className="flex items-center gap-3 text-sm font-semibold">Score mínim <select value={minScore} onChange={e=>setMinScore(Number(e.target.value))} className="rounded-xl border bg-background px-3 py-2"><option value={0}>Totes</option><option value={60}>60+</option><option value={70}>70+</option><option value={80}>80+</option></select></label></div></section><section className="mt-6 overflow-hidden rounded-2xl border bg-card shadow-sm"><div className="flex items-center gap-2 border-b p-5"><Radar size={19} className="text-emerald-600"/><div><h2 className="font-bold">Ranking d&apos;oportunitats</h2><p className="text-sm text-muted">Score, rendibilitat i senyals de negociació.</p></div></div><div className="overflow-x-auto"><table className="w-full min-w-[920px] text-left text-sm"><thead className="bg-background text-xs uppercase tracking-wide text-muted"><tr><th className="p-4">Vehicle</th><th className="p-4">Compra DE</th><th className="p-4">Venda ES</th><th className="p-4">Benefici / ROI</th><th className="p-4">Històric</th><th className="p-4">Score</th></tr></thead><tbody>{opportunities.map(item=><tr key={item.listing.id} className="border-t"><td className="p-4"><b>{item.listing.make} {item.listing.model}</b><p className="mt-1 text-xs text-muted">{item.listing.year} · {item.listing.mileageKm.toLocaleString("ca-ES")} km</p></td><td className="p-4">{euro(item.listing.price)}</td><td className="p-4">{euro(item.expectedSalePrice)}<p className="mt-1 text-xs text-muted">{item.marketDiscount.toFixed(1)} % diferència</p></td><td className="p-4"><b className="text-emerald-700 dark:text-emerald-300">+{euro(item.adjustedProfit)}</b><p className="mt-1 text-xs text-muted">{item.roi.toFixed(1)} % ROI</p></td><td className="p-4">{item.priceDrop>0?<span className="inline-flex items-center gap-1 font-bold text-emerald-700 dark:text-emerald-300"><TrendingDown size={15}/>-{euro(item.priceDrop)}</span>:<span className="text-muted">Sense canvis</span>}<p className="mt-1 text-xs text-muted">{item.daysListed} dies · {signal[item.negotiationSignal]}</p></td><td className="p-4"><span className="inline-flex min-w-12 justify-center rounded-full bg-emerald-50 px-3 py-1 font-bold text-emerald-700 dark:bg-emerald-950 dark:text-emerald-300">{Math.round(item.score)}</span><p className="mt-1 text-xs text-muted">{quality(item.score)}</p></td></tr>)}{opportunities.length===0&&<tr><td colSpan={6} className="p-8 text-center text-muted">Cap oportunitat amb aquest score mínim.</td></tr>}</tbody></table></div></section><section className="mt-6 rounded-2xl border bg-card p-5 shadow-sm"><div className="flex items-center gap-2"><Tag size={18} className="text-emerald-600"/><h2 className="font-bold">Històric preparat per dades reals</h2></div><p className="mt-2 text-sm leading-6 text-muted">Cada anunci conserva primer i últim avistament, així com observacions de preu. Amb providers autoritzats, aquest mateix sistema detectarà rebaixes, antiguitat de l&apos;anunci i senyals de negociació.</p></section></>}
